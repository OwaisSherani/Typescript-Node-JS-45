"use strict";
let user = ['Owais', 'Haris', 'Huzaifa', 'Anus', 'Admin'];
if (user.length === 0) {
    console.log("We need to find some users");
}
else {
    user = [];
    console.log("All user name has been removed", +user.length);
}
